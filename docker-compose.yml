version: '2'

services:
  servidor:
    build: php7_apache
    container_name: php7_apache
    ports:
      - "8090:80"
    volumes:
      - ./wwww:/var/www/html
      - ./httpd.conf:/usr/local/apache2/conf/httpd.conf
